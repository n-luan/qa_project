<div class="qa-a-selection">
  <%= form_for Answer.new, url: selections_path, remote: true do |f| %>
    <%= hidden_field_tag :answer_id, answer.id %>
    <%= f.submit class: "qa-a-select-button", value: "" %>
  <% end %>
</div>
